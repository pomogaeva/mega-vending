import { SimilarProdPipe } from './similar-prod.pipe';

describe('SimilarProdPipe', () => {
  it('create an instance', () => {
    const pipe = new SimilarProdPipe();
    expect(pipe).toBeTruthy();
  });
});
